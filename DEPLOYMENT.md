# Deployment

The first release is being validated for the existing ONCE host at `tasks.pocketcontext.com`. Deployment requires the private `taskcontext-backup` replica and the operator/R2 credentials configured in the sibling `once-pocketcontext/.envrc.private`. No live release is claimed until validation and deployment finish.
